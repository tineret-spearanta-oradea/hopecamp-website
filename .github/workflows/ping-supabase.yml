name: Ping Supabase to Prevent Pausing

on:
  schedule:
    # Runs at 9:00 AM UTC every Monday and Thursday
    # You can adjust the cron schedule as needed.
    # See: https://crontab.guru/
    - cron: '0 9 * * 1,4'
  workflow_dispatch: # Allows manual triggering from the GitHub Actions UI

jobs:
  ping_dev:
    name: Ping Dev Supabase
    runs-on: ubuntu-latest
    steps:
      # Step 1: Checkout the repository (optional, not strictly needed for this script)
      # - name: Checkout repository
      #   uses: actions/checkout@v4

      # Step 2: Set up Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18' # Use Node.js 18 or your preferred version

      # Step 3: Install Supabase Client
      - name: Install Supabase Client
        run: npm install @supabase/supabase-js

      # Step 4: Ping Dev Supabase Editions Table
      - name: Ping Dev Supabase Editions Table
        env:
          SUPABASE_URL: ${{ secrets.DEV_SUPABASE_URL }} # Your Dev Supabase project URL
          SUPABASE_KEY: ${{ secrets.DEV_SUPABASE_SERVICE_ROLE_KEY }} # Your Dev Supabase service role key
        run: |
          node -e "
          (async () => {
            try {
              // Import Supabase client
              const { createClient } = require('@supabase/supabase-js');

              // Check if environment variables are set
              const supabaseUrl = process.env.SUPABASE_URL;
              const supabaseKey = process.env.SUPABASE_KEY;

              if (!supabaseUrl || !supabaseKey) {
                throw new Error('Supabase URL or Key is missing in environment variables.');
              }

              console.log('Supabase URL:', supabaseUrl); // Log URL (consider security implications if sensitive)
              // Avoid logging the key itself for security reasons
              console.log('Dev Supabase Key: Present');

              const supabase = createClient(supabaseUrl, supabaseKey);

              // Ping Dev Supabase by querying the 'editions' table
              console.log('Pinging Dev Supabase editions table...');
              const { data, error } = await supabase.from('editions').select('id').limit(1);

              // Handle errors
              if (error) {
                console.error('Supabase query error:', error);
                throw error; // Re-throw the error to make the step fail
              }

              // Log success
              console.log('Dev Ping successful. Received data:', data);
            } catch (err) {
              // Log and exit with error
              console.error('Error pinging Dev Supabase:', err.message);
              process.exit(1); // Exit with a non-zero code to indicate failure
            }
          })();
          "

  ping_prod:
    name: Ping Prod Supabase
    runs-on: ubuntu-latest
    steps:
      # Step 1: Checkout the repository (optional, not strictly needed for this script)
      # - name: Checkout repository
      #   uses: actions/checkout@v4

      # Step 2: Set up Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18' # Use Node.js 18 or your preferred version

      # Step 3: Install Supabase Client
      - name: Install Supabase Client
        run: npm install @supabase/supabase-js

      # Step 4: Ping Prod Supabase Editions Table
      - name: Ping Prod Supabase Editions Table
        env:
          SUPABASE_URL: ${{ secrets.PROD_SUPABASE_URL }} # Your Prod Supabase project URL
          SUPABASE_KEY: ${{ secrets.PROD_SUPABASE_SERVICE_ROLE_KEY }} # Your Prod Supabase service role key
        run: |
          node -e "
          (async () => {
            try {
              // Import Supabase client
              const { createClient } = require('@supabase/supabase-js');

              // Check if environment variables are set
              const supabaseUrl = process.env.SUPABASE_URL;
              const supabaseKey = process.env.SUPABASE_KEY;

              if (!supabaseUrl || !supabaseKey) {
                throw new Error('Supabase URL or Key is missing in environment variables.');
              }

              console.log('Prod Supabase URL:', supabaseUrl); // Log URL (consider security implications if sensitive)
              // Avoid logging the key itself for security reasons
              console.log('Prod Supabase Key: Present');

              const supabase = createClient(supabaseUrl, supabaseKey);

              // Ping Prod Supabase by querying the 'editions' table
              console.log('Pinging Prod Supabase editions table...');
              const { data, error } = await supabase.from('editions').select('id').limit(1);

              // Handle errors
              if (error) {
                console.error('Supabase query error:', error);
                throw error; // Re-throw the error to make the step fail
              }

              // Log success
              console.log('Prod Ping successful. Received data:', data);
            } catch (err) {
              // Log and exit with error
              console.error('Error pinging Prod Supabase:', err.message);
              process.exit(1); // Exit with a non-zero code to indicate failure
            }
          })();
          "
